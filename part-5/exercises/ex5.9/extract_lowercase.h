#ifndef GUARD_extract_lowercase_h
#define GUARD_extract_lowercase_h

#include <vector>
#include <string>

std::vector<std::string> split(std::string);
std::vector<std::string> extract_lowercase(std::vector<std::string>&);

#endif
