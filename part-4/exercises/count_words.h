#ifndef GUARD_count_duplicates
#define GUARD_count_duplicates

#include <vector>
#include <string>

std::vector<int> count_words(std::vector<std::string> words, std::vector<std::string>& words_unique);

#endif
