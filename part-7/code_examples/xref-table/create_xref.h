#include <vector>
#include <map>
#include <string>
#include <istream>
#include "split_string.h"

std::map<std::string, std::vector<int>> xref(std::istream& in,
    std::vector<std::string> find_words(const std::string& s) = split);

